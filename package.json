{
  "name": "github-autopuller",
  "version": "1.0.0",
  "description": "A script used to autopull from GitHub via webhook - Without waiting for actions.",
  "main": "dist/index",
  "scripts": {
    "start": "node dist/index",
    "compile": "yarn --silent && rimraf dist && tsc",
    "dev": "yarn compile && yarn start"
  },
  "license": "ISC",
  "dependencies": {
    "child_process": "^1.0.2",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "@types/node": "^18.11.17",
    "rimraf": "^3.0.2",
    "typescript": "^4.9.4"
  }
}
